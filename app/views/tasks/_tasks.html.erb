  <table class="table table-striped">
    <thead>
      <tr>
        <th>タスク名</th>
        <th>ステータス</th>
      </tr>
    </thead>
    <tbody>
    <% @tasks.each do |task| %>
      <tr>
        <td><%= link_to task.content, task %></td>
        <td><%= task.status %></td>
      </tr>
    <% end %>
  </tbody>
  </table>
</ul>